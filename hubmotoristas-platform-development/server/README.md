# Hubmotoristas Backend (Local)

## Executar localmente

1. Copie `.env.example` para `.env`.
2. Inicie o servidor:

```bash
node server/index.js
```

API base: `http://localhost:4000/api`

Socket.io: `http://localhost:4000`